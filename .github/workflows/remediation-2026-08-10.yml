name: remediation-2026-08-10
on:
  push:
    branches:
      - remediation/kommunsign-full-product-completion-2026-08-10
permissions:
  contents: write
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: node --version
      - run: npm --version
      - run: test -f AGENTS.md
      - run: echo "workflow parsed and runner started"
